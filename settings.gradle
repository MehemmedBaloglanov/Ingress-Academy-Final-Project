pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }

    plugins {
        id 'org.springframework.boot' version '3.1.3'
    }
}

rootProject.name = 'FinalProjectLastVersion'

include ':Course-MS', ':Notification-MS', ':User-MS'
include 'Common'

